'use strict';
import React, { Component } from 'react';
import { RootContainer, HeaderOne } from './common';
import styled from 'styled-components/native';

export default class Dai extends Component {
  render() {
    return (
      <RootContainer>
        <HeaderOne>Dai</HeaderOne>
      </RootContainer>
    );
  }
}
